const
  LPAREN = /\(/
  RPAREN = /\)/
  LCURVY = /\{/
  RCURVY = /\}/
  COLON  = /:/
  INTEGER = /[0-9]+/
  STRING = /""/

queryParams = {
  own: Boolean,
  graph: {
    field,
    field
    __filter: value
  },
  eager: {
    field: {
      filter: {
        filtername: value,
        filterName2: value
      }
    }
  }
}

eager: {
  field: {
    fields
  }
}

{
  field
  field
  field
  field

}



{
  fields
  field
  filter
  filter
}