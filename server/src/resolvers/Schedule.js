module.exports = {
  // description: {
  //   resolve: (data, args, ctx, info) => {
  //     let desc = []

  //     if (data.offline1 && data.offline2 && data.offline1.categroy === data.offline2.category) {
  //       desc.push(`Día ${getCategoryLabel(data.offline1.category)}`)
  //     } else {
  //       if (data.offline1) {
  //         desc.push(`Mañana ${getCategoryLabel(data.offline1.category)}`)
  //       }
  //       if (data.offline2) {
  //         desc.push(`Tarde ${getCategoryLabel(data.offline1.category)}`)
  //       }
  //     }

  //     desc = desc.concat(data.timeline.reduce((timeline, element) => {
  //       timeline.push(``)
  //       return timeline
  //     }, []))

  //     return desc.join(', ')
  //   }
  // }
}
