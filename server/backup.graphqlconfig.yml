# currently renamed to see if anythign breaks
projects:
  app:
    schemaPath: src/schema/index.graphql
    extensions:
      endpoints:
        default: http://localhost:4000
  database:
    schemaPath: src/schema/generated/prisma.graphql
    extensions:
      prisma: prisma.yml
